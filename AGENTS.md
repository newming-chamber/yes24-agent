# yes24-agent

Yes24 AI 에이전트 — Yes24 사이트를 **라이브 소스**로 검색·열람해 인용 달린 답변을 스트리밍하는 ChatGPT/퍼플렉시티형 대화 어시스턴트. crema-ai(도서추천 파이프라인)의 후속 프로젝트이며 **자체 인덱스(Typesense) 없이** 동작한다.

**정체성 (2026-07-08 사용자 방향 확정)**: **유능한 범용 AI 어시스턴트가 기본**이고, 그 위에 **"Yes24 책·상품에 특히 밝은 페르소나"**를 얹은 구조 (도서 특화가 유일 범위가 아니라 강점). 뭐든 자연스럽게 답하되 책·상품·쇼핑은 Yes24 실시간 검색으로 전문성 발휘, 그 외(주가·날씨·영화·스포츠·시사·상식)는 web_search. **"Yes24에서 알 수 없어서/제 전문이 아니라서" 같은 변명·사과 서두 금지 — 질문에 바로 답한다.** (단 상품 가격·구매는 Yes24 출처만 인용, 인용 [n]·오늘날짜 시제는 불변.)

## 필독 문서

- `docs/PLAN.md` — 단계별 실행 계획 (레퍼런스 포함). **구현은 이 문서 순서대로.**
- `docs/spec.md` — 설계 스펙 (아키텍처·도구 명세·SSE 계약·리스크)
- `docs/progress-log.md` — 실시간 진행 로그 + **마일스톤 M1~M8 완료 체크리스트**(하단)
- `docs/known-limitations.md` — QA 실측 기반 알려진 한계·비결정 동작 이력 (재논의 시 `qa/reports/` 참조)

## 핵심 설계 결정 (재논의 불필요 — 근거는 spec.md)

1. **단일 루트 에이전트 루프 + 코드 오케스트레이션** (도구 5개: `yes24_search`·`yes24_fetch`·`yes24_browse`, `web_search`·`web_fetch`). 초기 "도구 2개 · 별도 라우터/게이트 없음"에서 **`docs/redesign-decision.md`(2026-07-08, 사용자 승인)로 진화** — 다단계 자율성은 프롬프트가 아니라 **모델 바깥 코드**로: 하이브리드 flash/pro 라우터(`routing.py`), 무출처/충분성 게이트(`product_gate`·`sufficiency_gate`), `correction_agent`(도구 강제 재진입 1회). 단 **대화형은 여전히 단일 순차 루프**(병렬 서브에이전트 아님, 재진입 1회 cap) — 병렬 sub_agents는 향후 Deep Research 모드만.
2. **웹서치는 원시 검색+열람으로 에이전트가 직접 종합** (2026-07-09 v4, 사용자 방향): `web_search`(**퍼플렉시티 `/search`**)는 **요약이 아닌 원시 검색결과**를 준다 — snippet 필드에 추출 본문(max_tokens_per_page로 분량 조절)과 `last_updated`(신선도)가 함께 담긴다(Tavily의 snippet/raw_content 이원 구조를 단일 필드로 통합). 더 긴 전문이 필요하면 `web_fetch`(Tavily `/extract`)로 특정 URL을 읽어, **에이전트가 직접 큐레이션·종합**한다. sonar 요약형은 가공을 도구에 가둬 폐기, Tavily /search도 퍼플렉시티로 교체(사용자 선호). 둘 다 벤더가 대신 fetch하므로 서버 SSRF 표면 없음. 상품 정보(가격·구매)는 여전히 Yes24 출처만. 웹 출처는 `type=web`. 시의성 질문은 last_updated 최신 우선으로 종합.
3. **대화 우선.** 잡담·후속질문은 도구 없이 즉답. 도구 호출 여부는 에이전트가 판단.
4. **인용은 구조로 강제.** 도구 결과마다 source_id 부여(세션 state 누적) → 답변 `[n]` 마커 → postprocess에서 마커↔출처 대조, 매핑 안 되는 인용 제거.
4a. **검증 가능한 상품 사실(제목·저자·가격·평점)은 이번 턴 도구 결과에만 근거한다** (2026-07-08 crema QA 발견). 도구 미호출·결과 없음이면 상품 사실을 생성하지 않는다(파라메트릭 지식으로 책·가격 창작 금지). 근거: 원칙 4는 `[n]` 마커만 검증하므로 **마커 없이 서술된 상품 사실·가격**은 검증망 밖으로 유출됐다(감정+추천에서 검색 누락 시 "그럴듯한 정가" 환각 — 실측 15,120·16,920 수렴). 프롬프트로 "무출처 상품 사실 금지"를 명시하고, 필요 시 postprocess에 가격토큰(`원`)·저자 감지 구조 가드(상품 출처 0인데 가격 있으면 재검색/차단)를 둔다 — 마커 검증과 동일하게 "구조로 강제".
4b. **최종 응답(done.text)은 단일 완결 텍스트다.** 프리앰블/중간 delta 중복 누적, 상태 라벨(event: status의 detail), 디버그·사고 발화의 본문 혼입 금지. 도메인 무관 출력 조립 계약(runner) — 실측 위반: 프리앰블 2~3중 반복, "진행 상태를 알려주는 UI 라벨이 표시됩니다" 누출.
5. **프레임워크 Google ADK `google-adk[db]==2.3.0` 핀 고정** (crema-ai 검증 버전; floating 시 silent 404 이력). 세션/멀티턴 히스토리는 ADK DatabaseSessionService(sqlite)로 영속.
6. **하드코딩 금지.** URL·UA·타임아웃·모델명·상한값 전부 `config.py`(pydantic-settings) 필드. 시크릿은 `.env`만 (crema-ai가 config에 키를 박아 유출한 전례 반복 금지).
7. **파서만은 POC여도 테스트 필수.** `tests/fixtures/search_sample.html`(실제 검색 HTML, 상품 24개) 기반 회귀 테스트. 파싱 0건은 빈 성공으로 위장하지 말고 에이전트에 명시적 실패 반환.

## 검증된 사실 (2026-07-07 실측 — 재확인 불필요)

- Yes24 검색 `GET /product/search?domain=ALL&query={q}` → SSR HTML 200 (JS 렌더링 불필요)
- robots.txt: `/product/search`, `/product/goods` 허용 (구경로 `/Goods/`·`/member/`만 차단)
- 고객센터 FAQ `/CSCenter/FAQ`는 302 → 정책은 시드 URL 맵으로 확보: `/Mall/Help/FAQ?faqGb=..&faqSubGb=..`(EUC-KR SSR)가 반품/취소/교환/배송 실제 정책 (`urls.py` `POLICY_SEED_URLS`, 인코딩 자동감지). 정책 질문은 Yes24 내부 페이지로 답변(외부 web_search 아님).
- **검색 결과 셀렉터 (fixture로 검증 완료)**:
  - 아이템: `ul#yesSchList li[data-goods-no]` (goods_no는 data 속성)
  - 제목+링크: `a.gd_name` (href는 `/product/goods/{id}` 상대경로 → 절대화 필요)
  - 저자: `span.authPub.info_auth` / 출판사: `span.authPub.info_pub` / 출간일: `span.authPub.info_date`
  - 가격: `em.yes_b` (쉼표 포함 문자열) / 평점: `.rating_grade em.yes_b`
- 파싱 범위: 상품 상세는 **주간 우수리뷰만 파싱**, 전체 리뷰 목록은 AJAX라 미지원.

## 환경

- Python >=3.10, `uv` 사용 (`uv sync`, `uv run pytest`)
- API 키: `GEMINI_API_KEY` (ADK는 `GOOGLE_API_KEY`를 기대 → 시작 시 매핑 필요. crema-ai의 `runtime_env.ensure_google_api_key_env` 패턴 참고)
- 서버 기본 포트 8010, 배포는 기존 DEV EC2에 컨테이너 추가 (crema-ai `deploy.sh`와 무관)

## 작업 규칙

- **git commit/push는 사용자 승인 후에만.** 임의 커밋 금지.
- **테스트·실측 후 커밋.** 성능/동작 주장은 실행 증거와 함께.
- 설계 결정을 사용자에게 되묻지 말고 합리적 디폴트로 진행 (큰 방향 전환만 확인).
- 가설로 분기 만들지 말 것 — 베이스라인 실행 후 관찰된 실패 모드에만 대응.
- **하드코딩 감사 2층 체계**: `tests/test_no_hardcoding.py` 가드가 매 pytest마다 URL·모델명·UA·시크릿 패턴을 자동 검사 (예외는 ALLOWLIST에 사유와 함께). 큰 웨이브 완료 시 hardcode-auditor 에이전트로 의미적 하드코딩(매직넘버·도메인 상수) 심층 감사.
- **에이전트 팀으로 일한다** (2026-07-14 사용자 원칙): 필요한 역할은 그때그때 **스폰**한다 — 구현(계층별)·감사(상시·계층별)·적대 검증·QA 도그푸딩·AI 심사관·성능 계측·커밋 담당. 테크리드가 직접 구현하지 말고 배분·검토·조율한다. **커밋 준비 완료 = 구현 + 전 계층 감사 + 적대 검증 + 라이브 QA까지 전부 끝난 상태**이며, 그전엔 커밋하지 않는다.
- **삭제 우선 · 비대화 금지** (2026-07-14 사용자 원칙): 수정은 코드를 **줄이는 방향**이 기본이다. 불필요해진 구조·필터·분기·죽은 코드는 과감히 삭제하고, 새 규칙을 얹기 전에 **기존 것을 지울 수 없는지 먼저 검토**한다. 상류 구조 신호(검색 파라미터·도구 결과 필드·인용 유무)로 해결되면 하류 텍스트 필터는 통째로 삭제한다 — 필터 다층·성장형 목록·같은 판정의 중복 구현이 대표 제거 대상. **하드코딩과 "그에 준하는 구조"(키워드 목록·블랙/화이트리스트·문구 매칭·사례별 예외)도 동일하게 지양**한다. 순증 diff는 정당화 대상이며, 웨이브 보고에 "삭제한 구조/줄 수"를 함께 적는다.
- 응답은 한국어.

## 마일스톤 현황 · 알려진 한계

- **마일스톤 M1~M8 완료 체크리스트** → `docs/progress-log.md` 하단 "마일스톤 체크리스트" (진행 이력 정본)
- **알려진 한계 (QA 실측·비결정 동작 이력)** → `docs/known-limitations.md` (재논의 시 `qa/reports/` 참조)
